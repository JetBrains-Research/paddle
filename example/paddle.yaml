descriptor:
  name: example
  version: 0.1.0

roots:
  sources:
    - src/main
  tests:
    - src/test

# https://github.com/pyenv/pyenv/wiki#suggested-build-environment
environment:
  path: .venv
  python: 3.8

# By default, any custom repository will have precedence over PyPI
repositories:
  #  - name: ml
  #    url: https://repo.labs.intellij.net/artifactory/api/pypi/intellij-ml/simple/
  #    default: False # If True, this would disable the global PyPI repo, and make <ml> the default fallback source

  - name: paper-analyzer
    url: https://repo.labs.intellij.net/artifactory/api/pypi/paper-analyzer/simple/
    secondary: True # Thus, <ml> still would be the primary source. But without such flag, this repo would have precedence over <ml>

requirements:
  - name: redis
    version: 3.5.3
    repository: pypi

plugins:
  repositories:
    - name: py-local
      dir: ./plugins/py

    #      - name: base
    #        url: https://some.ru
    #      - name: java
    #        jar: "./plugins/test-plugin-0.1.0.jar"

  enabled:
    embedded:
      - python
      - docker
      - ssh

    local:
      - name: greeting
        repository: py-local

      - name: descriptor-writer
        repository: py-local

#      py:
#        - name: greeting
#          version: 0.0.1
#          repository: base
#

tasks:
  run:
    - entrypoint: src/main/main.py
      id: main

# description writer plugin's configuration block
desc-writer:
  dir-name: desc-dir
  file-name: desc-file

#  remoter:
#    - script: digit_classifier.py
#      requirements: requirements.txt
#      other-files:
#        - weights.pth
#      input-file: data.txt
#      output:
#        directory: results
#        file: predicts.txt
#
#      machines:
#        - user: admin
#          password: secret
#          host: localhost
#          working-dir: /workdir
#          py-interpreter-dir: /python3
#
#      - user: admin
#        password: secret
#        host: otherhost
#        working-dir: /python3
#        py-interpreter-dir: /python3

#executor:
#  type: docker
#  image: python:3.9-slim

# executor:
#   type: ssh
#   user: admin
#   password: secret
#   host: localhost
#   directory: /project
